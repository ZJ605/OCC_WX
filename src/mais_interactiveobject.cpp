#include "mais_interactiveobject.h"



void MAIS_InteractiveObject::Compute(const Handle(PrsMgr_PresentationManager3d)& prsmgr, const Handle(Prs3d_Presentation)& prs, const Standard_Integer mode)
{
    //AIS_InteractiveObject::Compute(prsmgr, prs, mode);
}

void MAIS_InteractiveObject::ComputeSelection(const Handle(SelectMgr_Selection)& sel, const Standard_Integer mode)
{
}
